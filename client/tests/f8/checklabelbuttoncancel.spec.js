// Generated by Selenium IDE
const { Builder, By, Key, until } = require('selenium-webdriver');
const assert = require('assert');
require('chromedriver');

describe('Check label button cancel', function() {
  this.timeout(30000);
  let driver;
  let vars;
  beforeEach(async function() {
    driver = await new Builder().forBrowser('chrome').build();
    vars = {};
  });
  afterEach(async function() {
    await driver.quit();
  });
  it('Check label button cancel', async function() {
    await driver.get("http://localhost:4200/admin-user/list-department");
    await driver.manage().window().setRect({ width: 1936, height: 1056 });
    await driver.findElement(By.id("create_template_form")).click();
    await driver.findElement(By.name("Search")).click();
    await driver.findElement(By.css(".ant-table-cell-row-hover .ant-space-item:nth-child(2) > .ant-btn")).click();
    assert(await driver.findElement(By.css(".ant-btn-primary:nth-child(1) > span")).getText() == "キャンセル");
  });
});
