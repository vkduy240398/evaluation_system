// Generated by Selenium IDE
const { Builder, By, Key, until } = require('selenium-webdriver');
const assert = require('assert');
require('chromedriver');

describe('Check button add - field 部署コード -  field required', function() {
  this.timeout(30000);
  let driver;
  let vars;
  beforeEach(async function() {
    driver = await new Builder().forBrowser('chrome').build();
    vars = {};
  });
  afterEach(async function() {
    await driver.quit();
  });
  it('Check button add - field 部署コード -  field required', async function() {
    await driver.get("http://localhost:4200/admin-user/add-department");
    await driver.manage().window().setRect({ width: 1936, height: 1056 });
    await driver.findElement(By.css(".ant-btn-default")).click();
    await driver.findElement(By.css("#department_code_help > .ant-form-item-explain-error")).click();
    assert(await driver.findElement(By.css("#department_code_help > .ant-form-item-explain-error")).getText() == "この項目を入力してください。");
  });
});
