// Generated by Selenium IDE
const { Builder, By, Key, until } = require('selenium-webdriver');
const assert = require('assert');
require('chromedriver');

describe('Check default カテゴリー', function() {
  this.timeout(30000);
  let driver;
  let vars;
  beforeEach(async function() {
    driver = await new Builder().forBrowser('chrome').build();
    vars = {};
  });
  afterEach(async function() {
    await driver.quit();
  });
  it('Check default カテゴリー', async function() {
    await driver.get("http://localhost:4200/admin-user/add-department");
    await driver.manage().window().setRect({ width: 1936, height: 1056 });
    assert(await driver.findElement(By.css(".ant-radio-checked > .ant-radio-input")).isSelected());
    assert(!await driver.findElement(By.css(".ant-radio-wrapper:nth-child(2) .ant-radio-input")).isSelected());
  });
});
