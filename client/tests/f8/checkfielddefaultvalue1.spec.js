// Generated by Selenium IDE
const { Builder, By, Key, until } = require('selenium-webdriver');
const assert = require('assert');
require('chromedriver');

describe('Check field ロール - default value', function() {
  this.timeout(30000);
  let driver;
  let vars;
  beforeEach(async function() {
    driver = await new Builder().forBrowser('chrome').build();
    vars = {};
  });
  afterEach(async function() {
    await driver.quit();
  });
  it('Check field ロール - default value', async function() {
    await driver.get("http://localhost:4200/admin-user/list-user");
    await driver.manage().window().setRect({ width: 1936, height: 1056 });
    await driver.findElement(By.id("create_template_form")).click();
    await driver.findElement(By.xpath("//span[2]")).click();
    assert(await driver.findElement(By.xpath("//span[2]")).getText() == "すべて");
  });
});
