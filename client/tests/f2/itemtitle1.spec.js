// Generated by Selenium IDE
const { Builder, By, Key, until } = require('selenium-webdriver');
const assert = require('assert');
require('chromedriver');

describe('item title', function() {
  this.timeout(30000);
  let driver;
  let vars;
  beforeEach(async function() {
    driver = await new Builder().forBrowser('chrome').build();
    vars = {};
  });
  afterEach(async function() {
    await driver.quit();
  });
  it('item title', async function() {
    await driver.get("http://localhost:4200/home");
    await driver.manage().window().setRect({ width: 1900, height: 1020 });
    await driver.findElement(By.css(".ant-row:nth-child(3) .ant-typography")).click();
    await driver.findElement(By.css(".ant-layout-content")).click();
    await driver.findElement(By.xpath("//span[contains(.,\'公開専門スキル一覧\')]")).click();
    await driver.findElement(By.css("html")).click();
    await driver.findElement(By.css(".ant-typography")).click();
    await driver.findElement(By.css(".ant-typography")).click();
    assert(await driver.findElement(By.css(".ant-typography")).getText() == "公開専門スキル一覧");
    assert(await driver.findElement(By.css(".ant-form-item-no-colon")).getText() == "部署");
    assert(await driver.findElement(By.css(".main_button > span")).getText() == "検 索");
  });
});
