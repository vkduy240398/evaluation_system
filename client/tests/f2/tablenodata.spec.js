// Generated by Selenium IDE
const { Builder, By, Key, until } = require('selenium-webdriver');
const assert = require('assert');
require('chromedriver');

describe('table no data', function() {
  this.timeout(30000);
  let driver;
  let vars;
  beforeEach(async function() {
    driver = await new Builder().forBrowser('chrome').build();
    vars = {};
  });
  afterEach(async function() {
    await driver.quit();
  });
  it('table no data', async function() {
    await driver.get("http://localhost:4200/user/department-goal/1");
    await driver.manage().window().setRect({ width: 1936, height: 1056 });
    assert(await driver.findElement(By.css(".ant-table-placeholder > .ant-table-cell")).getText() == "該当データがありません");
  });
});
