// Generated by Selenium IDE
const { Builder, By, Key, until } = require('selenium-webdriver');
const assert = require('assert');
require('chromedriver');

describe('only add 10 rows', function() {
  this.timeout(30000);
  let driver;
  let vars;
  beforeEach(async function() {
    driver = await new Builder().forBrowser('chrome').build();
    vars = {};
  });
  afterEach(async function() {
    await driver.quit();
  });
  it('only add 10 rows', async function() {
    // Test name: only add 10 rows
    // Step # | name | target | value
    // 1 | open | http://localhost:4200/user/evaluation-8-10/5 | 
    await driver.get("http://localhost:4200/user/evaluation-8-10/5");

    // 2 | waitForElementPresent | xpath=//button[contains(.,'追 加')] | 30000
    await driver.wait(until.elementLocated(By.xpath("//button[contains(.,\'追 加\')]")), 30000);

    // 3 | click | xpath=//span[contains(.,'追 加')] | 
    await driver.findElement(By.xpath("//span[contains(.,\'追 加\')]")).click();

    // 4 | waitForElementPresent | xpath=//button[contains(.,'追 加')] | 30000
    await driver.wait(until.elementLocated(By.xpath("//button[contains(.,\'追 加\')]")), 30000);

    // 5 | click | xpath=//span[contains(.,'追 加')] | 
    await driver.findElement(By.xpath("//span[contains(.,\'追 加\')]")).click();

    // 6 | waitForElementPresent | xpath=//button[contains(.,'追 加')] | 30000
    await driver.wait(until.elementLocated(By.xpath("//button[contains(.,\'追 加\')]")), 30000);

    // 7 | click | xpath=//span[contains(.,'追 加')] | 
    await driver.findElement(By.xpath("//span[contains(.,\'追 加\')]")).click();

    // 8 | waitForElementPresent | xpath=//button[contains(.,'追 加')] | 30000
    await driver.wait(until.elementLocated(By.xpath("//button[contains(.,\'追 加\')]")), 30000);

    // 9 | click | xpath=//span[contains(.,'追 加')] | 
    await driver.findElement(By.xpath("//span[contains(.,\'追 加\')]")).click();

    // 10 | waitForElementPresent | xpath=//button[contains(.,'追 加')] | 30000
    await driver.wait(until.elementLocated(By.xpath("//button[contains(.,\'追 加\')]")), 30000);

    // 11 | click | xpath=//span[contains(.,'追 加')] | 
    await driver.findElement(By.xpath("//span[contains(.,\'追 加\')]")).click();

    // 12 | waitForElementPresent | xpath=//button[contains(.,'追 加')] | 30000
    await driver.wait(until.elementLocated(By.xpath("//button[contains(.,\'追 加\')]")), 30000);

    // 13 | click | xpath=//span[contains(.,'追 加')] | 
    await driver.findElement(By.xpath("//span[contains(.,\'追 加\')]")).click();

    // 14 | waitForElementPresent | xpath=//button[contains(.,'追 加')] | 30000
    await driver.wait(until.elementLocated(By.xpath("//button[contains(.,\'追 加\')]")), 30000);

    // 15 | click | xpath=//span[contains(.,'追 加')] | 
    await driver.findElement(By.xpath("//span[contains(.,\'追 加\')]")).click();

    // 16 | waitForElementPresent | xpath=//button[contains(.,'追 加')] | 30000
    await driver.wait(until.elementLocated(By.xpath("//button[contains(.,\'追 加\')]")), 30000);

    // 17 | click | xpath=//span[contains(.,'追 加')] | 
    await driver.findElement(By.xpath("//span[contains(.,\'追 加\')]")).click();

    // 18 | waitForElementPresent | xpath=//button[contains(.,'追 加')] | 30000
    await driver.wait(until.elementLocated(By.xpath("//button[contains(.,\'追 加\')]")), 30000);

    // 19 | click | xpath=//span[contains(.,'追 加')] | 
    await driver.findElement(By.xpath("//span[contains(.,\'追 加\')]")).click();

    // 20 | waitForElementPresent | xpath=//button[contains(.,'追 加')] | 30000
    await driver.wait(until.elementLocated(By.xpath("//button[contains(.,\'追 加\')]")), 30000);

    // 21 | click | xpath=//span[contains(.,'追 加')] | 
    await driver.findElement(By.xpath("//span[contains(.,\'追 加\')]")).click();

    // 22 | waitForElementPresent | xpath=//button[contains(.,'追 加')] | 30000
    await driver.wait(until.elementLocated(By.xpath("//button[contains(.,\'追 加\')]")), 30000);

    // 23 | click | xpath=//span[contains(.,'追 加')] | 
    await driver.findElement(By.xpath("//span[contains(.,\'追 加\')]")).click();

    // 24 | waitForElementPresent | xpath=//button[contains(.,'追 加')] | 30000
    await driver.wait(until.elementLocated(By.xpath("//button[contains(.,\'追 加\')]")), 30000);

    // 25 | click | xpath=//span[contains(.,'追 加')] | 
    await driver.findElement(By.xpath("//span[contains(.,\'追 加\')]")).click();

    // 26 | waitForElementVisible | css=.ant-message-custom-content > span:nth-child(2) | 30000
    await driver.wait(until.elementIsVisible(await driver.findElement(By.css(".ant-message-custom-content > span:nth-child(2)"))), 30000);

    // 27 | verifyText | css=.ant-message-custom-content > span:nth-child(2) | ko them qua 10 dong
    assert(await driver.findElement(By.css(".ant-message-custom-content > span:nth-child(2)")).getText() == "ko them qua 10 dong");
  });
});
